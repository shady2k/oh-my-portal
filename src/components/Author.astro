---
import { linkAttrs } from '../links.ts';
import { getAuthor } from '../site.ts';

/**
 * The author block — R5, at the end of every article.
 *
 * Design §3 puts it there for a reason worth keeping in mind while editing it:
 * the end of an article is the moment of peak reader interest, and today that
 * moment leads nowhere. So this carries the two things R3 and R4 ask for — a way
 * to make contact and the links — rather than a decorative byline.
 *
 * All of it comes from the content repository's `data/author.yaml`. Absent data
 * renders nothing rather than a placeholder.
 */
const author = await getAuthor();
---

{
  author && (
    <aside class="author" data-pagefind-ignore>
      <p class="field-label">автор</p>
      <div class="who">
        {/* alt="" because the name is printed right beside it: a screen reader
            would otherwise announce the same person twice in a row. */}
        {author.avatar && (
          <img class="avatar" src={author.avatar} alt="" width="56" height="56" loading="lazy" decoding="async" />
        )}
        <div>
          <p class="name">{author.name}</p>
          <p class="bio">{author.bio}</p>
        </div>
      </div>
      <p class="links utility">
        <a href={author.contact.href} {...linkAttrs(author.contact.href, Astro.site)}>{author.contact.label}</a>
        {author.links.map((link) => (
          <a href={link.href} {...linkAttrs(link.href, Astro.site, ['me'])}>
            {link.label}
          </a>
        ))}
      </p>
    </aside>
  )
}

<style>
  .author {
    margin-block-start: var(--space-4);
    padding-block-start: var(--space-3);
    border-block-start: 1px solid var(--rule);
  }

  p {
    margin: 0;
  }

  .who {
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
  }

  .avatar {
    flex: none;
    border-radius: 50%;
    object-fit: cover;
  }

  .name {
    font-weight: 600;
  }

  .bio {
    margin-block: var(--space-1) var(--space-2);
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
  }
</style>
